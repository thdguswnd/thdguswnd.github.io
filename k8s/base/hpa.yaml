apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: sm-be
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: sm-be
  minReplicas: 2
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
