apiVersion: v1
kind: ConfigMap
metadata:
  name: sm-be-config
data:
  # overlay 에서 프로파일/시크릿 참조를 구체화
  SPRING_PROFILES_ACTIVE: "gcp"
  # DB_URL, DB_USERNAME, DB_PASSWORD, GCS_BUCKET, INSTAGRAM_TOKEN 등 민감정보는
  # overlay 의 Secret/SecretProviderClass 로 주입 (ConfigMap 에 평문 금지)
