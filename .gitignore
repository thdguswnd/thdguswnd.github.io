# ────────────────────────────────────────
# Java / Gradle (sm-be)
# ────────────────────────────────────────
sm-be/build/
sm-be/.gradle/
sm-be/bin/
sm-be/out/
sm-be/*.class
sm-be/.idea/
sm-be/*.iml
sm-be/__temp__/

# ────────────────────────────────────────
# Node.js / Yarn (sm-fe)
# ────────────────────────────────────────
sm-fe/node_modules/
sm-fe/.pnp
sm-fe/.pnp.js
sm-fe/build/
sm-fe/dist/
sm-fe/.yarn/cache
sm-fe/.yarn/unplugged
sm-fe/.yarn/install-state.gz

# ────────────────────────────────────────
# 배포 아카이브 / 대용량 (커밋 금지)
# ────────────────────────────────────────
*.zip
*.tgz
# 웨딩촬영 원본(보정본 JPEG, 장당 10~19MB). 최적화된 webp 만 커밋한다.
# → sm-fe/src/assets/{gallery,timeline}/ 및 main.webp
webphotos/

# ────────────────────────────────────────
# 로컬 도구 설정
# ────────────────────────────────────────
.claude/

# ────────────────────────────────────────
# IDE
# ────────────────────────────────────────
.idea/
*.iml
.vscode/settings.json
.vscode/launch.json
*.suo
*.user

# ────────────────────────────────────────
# OS
# ────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V5
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ────────────────────────────────────────
# 환경 변수 / 시크릿
# ────────────────────────────────────────
.env
.env.local
.env.*.local

# ────────────────────────────────────────
# Git 로컬 설정 / 자격증명 (push 금지)
# ────────────────────────────────────────
.git-credentials
.netrc
.gitconfig.local
.github-token

# ────────────────────────────────────────
# AI-DLC 임시 파일
# ────────────────────────────────────────
aidlc-output/**/__temp__/

# 일회성 검증/작업 스크립트.
# 워크스페이스 밖(%TEMP%)에 쓰면 IDE 스냅샷 경로가 깨지므로 여기에 둔다.
.tmp/
